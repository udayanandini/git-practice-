pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'jenkins Build the pipeline'
            }
        }
        stage('Test') {
            steps {
                echo 'jenkins Test the pipeline'
            }
        }
        stage('Deploy') {
            steps {
                echo 'jenkins Deploy the pipeline'
            }
        }
    }
}
